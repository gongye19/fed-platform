# 项目进展

## T1 移除旧生成入口并刷新前端部署

- 类型：Maintenance（正式维护）
- 目标：确保版本管理只允许选择站点数据生成，线上不再显示“从空白生成”；关联 C5。
- 修改位置：L1；S3。
- 状态：已完成
- 开始时间：2026-09-11 12:51 HKT
- 完成时间：2026-09-11 12:56 HKT

### 步骤

1. 核对源码、构建产物和线上静态资源中的旧入口。
2. 确认生成请求只提交站点 Submission，基础版本由平台自动选择。
3. 重部署前端以替换用户浏览器仍加载的旧构建。
4. 运行前端测试和线上文字检查。

### 修改

- 当前源码无需再删除入口；它已只保留站点数据选择，生成基础版本由后端自动继承。
- 强制重新部署 `fed-console`，替换旧浏览器页面对应的线上构建。

### 验证

- `npm test -- --run`：9 项通过。
- `npm run build`：通过。
- 线上最新 JS 检索：不包含“从空白生成”或“空白生成”。

### 清理

- 未新增临时文件；保留版本谱系中的内部 `base_release_id`，因为增量联邦依赖它。
